package com.yanjinbin.leetcode;

public class CF {
    /*public boolean dfs(int n, int size, int cur) {
        if (size == 3) {
            return ok;
        }
        for (int i = cur; i < n; i++) {
            sub.addLast(cur);
            if (n % cur == 0 && dfs(n / cur, size + 1, cur++))
                return true;
            sub.pollLast(cur);
        }
    }*/
    
    
}